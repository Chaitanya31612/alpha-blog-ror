<div class="container">

  <div id="users">
    <% @users.each do |user| %>
      <div class="row justify-content-md-center">
        <div class="col-md-8">
          <div class="card border-5 text-center mb-5 bg-white rounded" id='article-card'>
            <div class="card-header">
              <i class="text-muted"><%= link_to user.username, user %></i>
            </div>
            <div class="card-body">
              <h5 class="card-title"><%= link_to gravatar_for(user, size: 150), user, class: 'text-decoration-none' %></h5>
              <p class="card-text"><%= pluralize(user.articles.count, 'article') %></p>
              <%= link_to 'View Profile', user, class: 'btn btn-outline-success' %>
              <% if logged_in? && user == current_user %>
                <%= link_to 'Edit Profile', edit_user_path(user), class: 'btn btn-outline-primary' %>
              <% end %>
            </div>
            <div class="card-footer text-muted">
              <small><i>Joined <%= time_ago_in_words(user.created_at) %> ago</i></small>
            </div>
          </div>
        </div>
      </div>
    <% end %>

  </div>
</div>