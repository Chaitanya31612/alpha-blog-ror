<%= render 'shared/errors', obj: @user %>
<%= form_with(model: @user, local: true) do |f| %>
  <div class="px-3">
    <div class="form-group row mb-2">
      <%= f.label :username, class: "col-2 col-form-label fw-semibold text-muted ps-0" %> <br>
      <%= f.text_field :username, class: "form-control", placeholder: "username_123" %>
    </div>
    <div class="form-group row mb-2">
      <%= f.label :email, class: "col-2 col-form-label fw-semibold text-muted ps-0" %> <br>
      <%= f.text_field :email, rows: 10, class: "form-control", placeholder: "email@example.com" %>
    </div>
    <div class="form-group row mb-2">
      <%= f.label :password, class: "col-2 col-form-label fw-semibold text-muted ps-0" %> <br>
      <%= f.password_field :password, rows: 10, class: "form-control", placeholder: "*******" %>
    </div>
    <div class="form-group row">
      <%= f.submit 'Sign Up', class: "btn btn-primary mt-4" %>
    </div>
  </div>
<% end %>